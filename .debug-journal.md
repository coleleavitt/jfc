# Debug Journal - rustc proc-macro segfault
Started: 2026-06-18T00:00:00-07:00
Goal: Complete the jfc-agent architecture work and resolve or isolate cargo build failures.

## Environment snapshot
- Runtime: Rust workspace, cargo build/test.
- Entry: `cargo build -p jfc-engine`, later `cargo build --workspace` and `cargo test --workspace`.
- Git HEAD: unknown at journal creation.
- References read:
  - `omo:programming/SKILL.md`
  - `omo:programming/references/rust/README.md`
  - `omo:debugging/SKILL.md`
  - `omo:debugging/references/runtimes/rust.md`
  - `omo:debugging/references/methodology/00-setup.md`
  - `omo:debugging/references/methodology/02-investigate.md`

## Hypotheses
1. [OPEN] Global cargo linker config (`peony`) produces proc-macro dylibs that rustc segfaults while loading.
   - Distinguishing evidence: build succeeds when linker/rustflags are bypassed and fails with default global config.
   - If true, fix is: bypass linker.
2. [OPEN] sccache is serving corrupt proc-macro artifacts.
   - Distinguishing evidence: build succeeds with `RUSTC_WRAPPER=""` after removing proc-macro `.so` artifacts.
   - If true, fix is: disable cache.
3. [OPEN] `target-cpu=native` / global rustflags trigger a rustc or LLVM codegen bug for proc macros.
   - Distinguishing evidence: build succeeds when rustflags are cleared but fails with default global rustflags.
   - If true, fix is: clear rustflags.

## Artifacts to revert
- [ ] `.debug-journal.md` - remove before final cleanup.

## Findings
- Prior observed failures: rustc SIGSEGV occurred while loading proc-macro dylibs including `libdisplaydoc`, `libref_cast_impl`, `libtracing_attributes`, and `libboa_macros`.
- Prior observed control: `cargo build -p jfc-agent` and `cargo test -p jfc-agent` passed.
