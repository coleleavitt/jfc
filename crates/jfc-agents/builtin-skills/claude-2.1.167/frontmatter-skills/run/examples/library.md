# Library Run Recipe

Exercise the package boundary rather than private source files. Use the public crate, package, module export, or documented sample command from a small external example.

Capture the exact public API call, returned value, and any error text. If the library has no runtime surface beyond tests, report that instead of pretending an internal import is an app run.

