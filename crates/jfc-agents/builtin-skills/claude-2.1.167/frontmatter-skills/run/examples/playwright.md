# Browser Run Recipe

Start the web app with an isolated port. Wait for the page to load with Playwright, then interact with the real UI route or component reached by the change.

Capture a screenshot or the relevant DOM text after the interaction. Check browser console errors before ending the run.

