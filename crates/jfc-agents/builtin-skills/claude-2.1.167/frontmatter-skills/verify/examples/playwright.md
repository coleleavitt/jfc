# Browser Verification Recipe

Start the app, open the relevant route in Playwright, and interact through visible controls rather than private APIs.

Capture a screenshot or DOM text that proves the changed behavior. Check console errors and probe one adjacent invalid state, resize, or repeated action when relevant.

